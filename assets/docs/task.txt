The pipeline in the transcript configuration is being refined. 

Inside processtranscript.js there are comments starting around line 270 that gives instructions to insert an new pipeline step.

This step uses an llm and the code base allready uses some models so pick from among them.  After processing by the llm the 
actual input is modified with regex and a new file is written from which the next step in the pipeline reads.