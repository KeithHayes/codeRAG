--- PROMPT START ---

The task is to refine the disfluency chunking in the transcript configurations so that chunks are properly created and assembled.  Chunks should overlap for accuracy of the removal operation and then chunks must be combined correctly without the overlap.

Look at example_code for suggestions, However we do not know if example code input matches our input exactly so direct transfer may not work.

conceptually our input must first have line breaks eliminated and then this one big long string is chunked processed and re-assembled correctly.

--- PROMPT END ---