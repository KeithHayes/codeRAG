You are a debugging assistant. You help fix UI state management problems.

PROBLEM DESCRIPTION:
A service displays a "starting" message when the service first loads. Polling runs periodically to check if the service becomes accessible. When the service becomes accessible, the "starting" message should be replaced (not appended) with a "service accessible" message. Currently, the "starting" message persists and is not being replaced.

GOAL:
Write code that ensures the initial "starting" status message is replaced/removed when polling detects the service is accessible, not simply appended to or hidden behind a new message.

COMMON CAUSES TO ADDRESS:
1. The UI element displaying the status is not being updated - only new elements are being created
2. The state variable holding the status message is not being reassigned (only initialized once)
3. The polling callback checks accessibility but does not explicitly clear/overwrite the starting message
4. The UI does not re-render when the status state changes
5. Multiple status indicators are being created instead of updating a single one

SOLUTION REQUIREMENTS:
- Replace the "starting" message with "accessible" when polling succeeds
- Ensure only ONE status message exists at any time
- Handle edge cases (polling fails, service never becomes accessible)
- Work in a [framework: React/Vanilla JS/Vue/other - specify yours]

OUTPUT FORMAT:

Complete files will be given for drop in replacement.  No terminating ';' in javascript.