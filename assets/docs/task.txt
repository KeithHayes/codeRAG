The pipeline in the 'transcript' configuration is being copied.

The new configuration will be named 'interview' and it will be placed on top of 'transcript' in the dropdoen.

Both configuations will do exactly the same thing.

Make no other changes other than that needed to clone this new parallel configuration.  The new javascript file for this interview configuration will call the same php functions that the transcript configuration javascript does.

The new js file for the configuration is named assets/js/processinterview.js

assets/yaml/interview.yaml is also created.  Other files are shared.  ???  does that need new .py files if we 
require duplicating the yaml if so then we need new files.



PRESERVE all existing logic and functionality EXCEPT that needed to implement the clone.  In time the configurations can be seperatly modified.  Stay on task.
