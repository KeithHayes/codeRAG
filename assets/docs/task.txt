

The load model 'Load Model' toolbar button in toolbar.js has a few problems.  It should show the loading model message until the model is fully loaded.  There should not be a timeout.  The loading is also unreliable.