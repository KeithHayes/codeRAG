The pipeline in the transcript configuration is being refined. 

The steps up to the execution of these stages are not to be changed. 

stages:
  - name: 'remove_extra_whitespace'
    type: 'regex'
    pattern: '\n\s*\n+'
    replacement: '\n\n'
    flags: 'g'
    enabled: true

  - name: 'format_paragraphs'
    type: 'llm'
    model: 'deepseek-r1:7b'
    system_prompt: "You are a transcript formatting assistant. Output ONLY the formatted text. No JSON, no markdown, no code blocks, no explanations."
    user_prompt_template: "Format this transcript into clean paragraphs. Output ONLY plain text:\n\n{input}"
    temperature: 0.1
    max_tokens: 32769
    enabled: true

  - name: 'clean_llm_json_artifacts'
    type: 'regex'
    pattern: '```[\s\S]*?```|```|`|\n{3,}'
    replacement: ''
    flags: 'g'
    enabled: true

Code for these stages will be rewritten so that the chunking 
over input is repeated.  This allows transcripts of arbitrary length to be processes.  Files will be created to match the pattern of previous steps.

to replace 

  - name: 'remove_extra_whitespace'
    type: 'regex'
    pattern: '\n\s*\n+'
    replacement: '\n\n'
    flags: 'g'
    enabled: true 

save_remove_extra_labels.php 

is a new file that saves the file 

sansextrasegments.txt in place of 

stage_remove_extra_whitespace.txt 

which will no longer be created.   

a python file named remove_extra_labels.py will apply 

    type: 'regex'
    pattern: '\n\s*\n+'
    replacement: '\n\n'

over the chunked inputs and then assemble the cleaned output back from the chunks.  

This python is called from processtranscript.js which uses save_remove_extra_labels.php to save the output.

to replace  

 - name: 'format_paragraphs'
    type: 'llm'
    model: 'deepseek-r1:7b'
    system_prompt: "You are a transcript formatting assistant. Output ONLY the formatted text. No JSON, no markdown, no code blocks, no explanations."
    user_prompt_template: "Format this transcript into clean paragraphs. Output ONLY plain text:\n\n{input}"
    temperature: 0.1
    max_tokens: 32769
    enabled: true

save_format_paragraphs.php 

is a new file that saves the file 

formattedparagraphs.txt in place of 

stage_format_paragraphs.txt

which will no longer be created.   

a python file named format_paragraphs.py will apply 

 - name: 'format_paragraphs'
    type: 'llm'
    model: 'deepseek-r1:7b'
    system_prompt: "You are a transcript formatting assistant. Output ONLY the formatted text. No JSON, no markdown, no code blocks, no explanations."
    user_prompt_template: "Format this transcript into clean paragraphs. Output ONLY plain text:\n\n{input}"
    temperature: 0.1
    max_tokens: 32769
    enabled: true

This python is called from processtranscript.js which uses save_format_paragraphs.php to save the output.

to replace  

  - name: 'clean_llm_json_artifacts'
    type: 'regex'
    pattern: '```[\s\S]*?```|```|`|\n{3,}'
    replacement: ''
    flags: 'g'
    enabled: true

save_cleaned_output.php 

is a new file that saves the file 

cleaned_output.txt in place of 

stage_clean_llm_json_artifacts.txt

which will no longer be created.   

a python file named clean_artifacts.py will apply 

  - name: 'clean_llm_json_artifacts'
    type: 'regex'
    pattern: '```[\s\S]*?```|```|`|\n{3,}'
    replacement: ''
    flags: 'g'
    enabled: true

This python is called from processtranscript.js which uses save_cleaned_output.php to save the output.

Respect my choice of filenames even if the code in the python files doing the processing does not match the name.


the final yaml will be 

toolbar:
  - 'fileload'
  - 'homeserver'
  - 'pastetranscript'
  - 'runtask'
  - 'book'
  - 'target'

ollama_model: "deepseek-r1:7b"

as the funtionalitiy of the stages is moved to code to match the pattern of the implemented pipeline which is not to be changed.

all functionality is moved into .py files called by the javascript runner and files are saved by .php files.
