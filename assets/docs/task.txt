PROMPT

Context:
I have two files from an older commit (after a git hard reset):

    processtranscript.js – currently outputs JSON.

    transcript.yaml – defines processing stages, currently oriented toward JSON extraction.

The goal is to transform raw transcript text (containing timestamps, repetitive definition blocks, and scattered speaker lines) into a clean, readable conversation – exactly like the formatted transcript shown below:

    Reason to Resist – Episode Transcript

    Host: Demetri Lceras (from Kalamata, Greece)
    Guest: Laith Maru (Executive Director of Free Palestine TV, Beirut, Lebanon)
    Date: April 27, 2026

    Demetri: Good day. This is Demetri Lceras coming to you from Kalamata, Greece...
    Laith: Good to be with you, Demetri...
    (etc.)

Requirements for the rewritten files:

    No JSON output – the final output must be plain text, formatted as a coherent dialogue.

    Multiple stages – the pipeline must break the task into separate, sequential stages (e.g., remove definition blocks, clean timestamps, extract speaker turns, merge split lines, add header).

    Multiple models – each stage must be able to use a different model (e.g., regex for simple removal, GPT-4o for speaker extraction, Claude for formatting, Gemini for correction). The YAML config must specify the model per stage.

    Input flexibility – the script should accept raw transcript from stdin or a file.

    Output – clean transcript text printed to stdout.

What you must do with the old files:

    Rewrite processtranscript.js to:

        Read the YAML pipeline configuration.

        Execute each stage in order.

        For regex stages: apply pattern replacement.

        For LLM stages: make API calls to the specified model (OpenAI, Anthropic, Gemini, or local via Ollama). Include placeholder functions that can be easily swapped with real API keys.

        Never output JSON; only print the accumulated text after all stages.

    Rewrite transcript.yaml to define at least these stages (you can add more):

        remove_definition_blocks – use regex to delete lines like [808] The "Zionist colony" refers to...

        clean_timestamps – remove lines like 0:13, 13 seconds, 1 minute, 1 second.

        extract_speaker_lines – use an LLM (GPT-4o-mini) to identify which lines belong to Demetri vs. Laith, normalize speaker names, and remove all non‑dialogue text.

        merge_and_format – merge split sentences, ensure proper punctuation, add blank lines between speaker turns.

        add_header – prepend the title, host/guest, date (extracted from transcript or set manually).

    Provide clear comments and instructions in both files so that anyone can adapt the models (e.g., switch from OpenAI to local Ollama) without breaking the pipeline.

Example of a raw input fragment (what the old pipeline fails to clean):
text

"Zionist colony" refers to Israel's occupation of Palestinian territory. [808] The "Caspian Sea" is a body of water... [809] The "Axis of Resistance" is a term...
0:13
13 seconds
Demetri: Good day. This is Demetri Lceras...
0:20
20 seconds
Laith: Good to be with you...

Expected output for that fragment:
text

Demetri: Good day. This is Demetri Lceras...
Laith: Good to be with you...

Constraints:

    Do not hardcode model names in the JavaScript; read them from the YAML.

    Handle API rate limits and errors gracefully (e.g., fallback to regex if LLM fails).

    The final output must be indistinguishable from the manually cleaned transcript shared earlier.

Your task:
Rewrite processtranscript.js and transcript.yaml exactly as described. Provide the complete code for both files. Use placeholders for API keys (e.g., process.env.OPENAI_API_KEY). Ensure the script works in a Node.js environment with js-yaml installed.
